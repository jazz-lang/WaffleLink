#[derive(Copy, Clone, PartialEq, Eq)]
pub enum Ins {
    Int(i32),
    Null,
    Undefined,
    Const(u32),
    Pop,
    PopN(u32),
    PopNArray(u32),
    PopNArrayRev(u32),

    GetStack(u32),
    SetStack(u32),

    GetById(u32),
    SetById(u32),
    GetByVal,
    SetByVal,
    GetUp(u32),
    SetUp(u32),
    GetThis,
    SetThis,
    Close(u32),
    Call(u32),
    ObjCall(u32),
    TailCall(u32),
    ObjTailCall(u32),
    New(u32),
    NewObject,
    NewArray(u32),
    Return,

    Br(u16),
    BrCond(u16, u16),
    LoopHint,
    Safepoint,
    Add,
    Sub,
    Div,
    Mul,
    Rem,
    Shr,
    Shl,
    UShr,
    Eq,
    NEq,
    Gt,
    Lt,
    Ge,
    Le,
}
